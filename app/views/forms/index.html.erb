<div class="background">
  <div class="main-container">
    <div class="sidebar">dashboard</div>
    <div class="form-display">
      <div class="card-grid">
        <% @forms.each do |form| %>
          <%=link_to "", form_path(form), class:"card-index"%>  
        <% end %>
      </div>
    </div>
  </div> 
</div>